services:
  backend:
    build: ./backend
    container_name: pland-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PORT=8181
    networks:
      - pland

  frontend:
    build:
      context: ./frontend
      args:
        - REACT_APP_API_URL=${REACT_APP_API_URL}
    container_name: pland-frontend
    restart: unless-stopped
    networks:
      - pland
      - adam

networks:
  pland:
    driver: bridge
  adam:
    external: true
